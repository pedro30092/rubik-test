{
  "name": "rubik-backend",
  "version": "1.0.0",
  "description": "Exam session API — Phase 1 backend",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
